# Normalize all text files to LF in the repo and on checkout (prevents CRLF drift)
* text=auto eol=lf

# Binary files — never apply line-ending conversion
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.ico  binary
*.woff  binary
*.woff2 binary
*.ttf  binary
*.eot  binary
*.pdf  binary
